import React from 'react';
import Fieldset from './Fieldset';

class Form extends React.Component {
  render() {
    return (
      <form>
        <Fieldset />
      </form>
    );
  }
}

export default Form;
